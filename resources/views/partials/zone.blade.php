{{-- resources/views/partials/zone.blade.php --}}
<div id="{{ $id }}" class="zone-card card bg-dark text-white border-warning shadow">
    <div class="zone-title bg-warning text-dark fw-bold text-center py-2">{{ $title }}</div>
    <div class="zone-rule text-muted text-center small py-1">{{ $rule }}</div>
    <div class="chip-container"></div>
</div>